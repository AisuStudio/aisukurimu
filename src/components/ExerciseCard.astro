---
/*
  Exercise card — Brand v2.
  16:9 image slot on top, body with lemon caption + vanilla description +
  raspberry tag list. Border 1px raspberry, hover-lift -2px.
*/
import { url } from '../lib/url';

interface Props {
  href: string;
  title: string;
  description: string;
  category: string;
  level: number;
  image?: string;
  tags?: string[];
}

const { href, title, description, category, level, image, tags = [] } = Astro.props as Props;
const imageSrc = image ? url(image) : null;
---

<a class="ex-card" href={href}>
  {imageSrc ? (
    <img class="ex-image" src={imageSrc} alt="" />
  ) : (
    <div class="ex-image placeholder-image" role="img" aria-label="Platzhalter"></div>
  )}
  <div class="ex-body">
    <h3 class="h2-lcontainer ex-caption">{title}</h3>
    <p class="p-small-vanilla ex-desc">{description}</p>
    {tags.length > 0 && (
      <p class="caption ex-tags">{tags.slice(0, 4).join(', ')}</p>
    )}
    <p class="caption ex-meta">{category} · Level {level}</p>
  </div>
</a>

<style>
  .ex-card {
    display: flex;
    flex-direction: column;
    background: transparent;
    border-radius: 4px;
    text-decoration: none;
    color: inherit;
    overflow: hidden;
    transition: transform 0.18s ease;
  }

  .ex-card:hover,
  .ex-card:focus-visible {
    transform: translateY(-2px);
  }

  .ex-image {
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    display: block;
  }

  .ex-body {
    padding: var(--sp-lg);
    display: flex;
    flex-direction: column;
    gap: var(--sp-md);
    background: rgba(31, 25, 52, 0.8);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-radius: 0 0 4px 4px;
  }

  .ex-caption { color: var(--ak-vanilla) !important; }
  .ex-desc { color: var(--ak-vanilla) !important; }
  .ex-tags { color: var(--ak-lemon) !important; }

  /* Smaller variant of h2-lcontainer for use inside cards */
  .ex-caption {
    font-size: 16px;
    line-height: 20px;
  }

  .ex-desc { /* Inherits .p-small-vanilla styling */ }
  .ex-tags { margin: 0; }
  .ex-meta { margin: 0; color: var(--ak-cappuccino) !important; opacity: 0.9; }
</style>
